import { Card } from '@/components/Card';
import Image from 'next/image';

const Home = () => {
    return (
        <main>
            Xin chào
            <Card />
        </main>
    );
};

export default Home;
